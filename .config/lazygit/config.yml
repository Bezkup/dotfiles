gui:
  sidePanels:
    - [files, branches, commits]
  showCommandLog: false
git:
  diffRenderers:
    - name: delta
      colorArg: always
      command: delta --dark --paging=never
    - type: rawGit
      name: default
